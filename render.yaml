services:
  - type: web
    name: metrocity-tickets
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "4040"
      - key: TICKET_HMAC_SECRET
        generateValue: true
